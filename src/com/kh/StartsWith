/*
맨 앞에 boolean 가능
startsWith(String prefix) = prefix로 시작글자 있나 확인
boolean startWord = String으로 든 변수명.startsWith("시작글자");


endWith(String suffix) = suffix로 끝나는 글자 있나 확인
boolean endWord = String으로 든 변수명.endWith("끝글자");
파일쓰기 = File(경로와 파일이름 가져오기) FileWriter(가져온 파일에 작성)
파일읽기 = FileReader(읽어올 파일 경로와 이름 가져오기) BufferedReader(FileReader 돕는다)
FileWriter FileReader BufferedReader 는 트라이 안에 존재
prefix 시작하는, 접두사

suffix 끝나는, 접미사 / 언어 사

윈도우는 숨겨진 공백이 있어 trim() 로 양쪽 공백 제거해야한다
\r = 왼쪽끝으로 키보드 커서 이동









*/